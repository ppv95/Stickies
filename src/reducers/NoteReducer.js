import { types } from "../types/types";

export const NoteReducer = (state,action) => {

    switch (action.type) {
        case types:
            
            break;
    
        default:
            break;
    }
}  